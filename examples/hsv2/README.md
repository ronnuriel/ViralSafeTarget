# HSV-2 example

Real HSV-2 genomes are downloaded locally rather than committed to Git.

Run:

```bash
bash scripts/run_real_hsv2.sh --sample-size 25
```

The current pilot uses reference accession `NC_001798.2`. Before publication,
freeze the accession list and inspect HSV repeat/isomer alignment quality.
